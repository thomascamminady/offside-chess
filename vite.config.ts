import { defineConfig } from "vite";

// Served from https://<user>.github.io/offside-chess/ — update this if the
// repository is renamed, or set it to "/" for a user/org root-page repo.
export default defineConfig({
  base: "/offside-chess/",
});
